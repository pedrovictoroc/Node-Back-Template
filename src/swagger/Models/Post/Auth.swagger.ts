export const PostAuth = {
    "required": ["document", "password"],
    "properties": {
        "document": {
            "type": "string"
        },
        "password": {
            "type": "string",
        }
    }
}